import React from 'react';
import { StyleSheet, Text, SafeAreaView, ScrollView, StatusBar, View, Button, Pressable } from 'react-native';

const Plot = (props) => {
  return (
    <View style={styles.window} >

    </View>
  )
}

const styles = StyleSheet.create({
  window: {
    marginHorizontal: 0,
    marginVertical: 10,
    backgroundColor: "#21292c",
    width: "100%",
    height: "30%"
  }
});
